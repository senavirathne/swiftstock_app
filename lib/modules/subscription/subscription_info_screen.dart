// lib/modules/subscription/subscription_info_screen.dart

import 'package:flutter/material.dart';

class SubscriptionInfoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Subscription Info'),
      ),
      body: Center(
        child: Text('Subscription information will be displayed here.'),
      ),
    );
  }
}
